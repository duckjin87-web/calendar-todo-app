# 일정 · 투두 캘린더 (프로토타입)

아웃룩 연동 e-ink 탁상 캘린더 디바이스를 위한 프론트엔드 프로토타입입니다.
현재는 하드웨어/백엔드 없이 **더미 데이터**로 동작하는 웹 목업 단계입니다.

**흑백 e-ink 전자책 단말기(예: 이노스페이스원 루나2 · 6인치 · 1072×1448 · 세로 3:4)** 에 최적화되어 있습니다.
- 다크 여백/카드 테두리를 없애 화면을 꽉 채우고, 폰트·요소 크기를 전체적으로 확대
- 중요도 색과 형광펜 강조를 **그레이스케일 명도 단계**로 재설계해 흑백 화면에서도 뚜렷하게 구분
- PWA 지원: 홈 화면에 **앱처럼 설치**해서 오프라인으로 사용 가능

## 실행 방법
별도 빌드 없이 `index.html`을 브라우저로 열면 바로 동작합니다.
```bash
# 정적 서버로 실행하고 싶다면 (선택)
npx serve .
```
> PWA(서비스워커) 설치 기능은 `https://` 또는 `localhost`에서만 동작합니다. 파일을 더블클릭해 `file://`로 열면 화면은 나오지만 앱 설치는 안 됩니다.

## 파일 구성
```
index.html          레이아웃 뼈대 + PWA 메타/서비스워커 등록 + Firebase 로드
style.css           전체 스타일 (루나2 흑백 최적화)
script.js           상태/렌더링 로직 + 데이터 계층(Firestore/로컬) + 수정·삭제·역할모드
manifest.json       PWA 앱 정보(이름·아이콘·표시 모드)
sw.js               오프라인 캐시용 서비스워커
firebase-config.js  Firebase 설정값(사용자가 채움. 비우면 로컬 모드)
firestore.rules     Firestore 보안 규칙
icon-192.png / icon-512.png / apple-touch-icon.png   앱 아이콘
```

## 기기 모드(편집/읽기)
화면 상단의 **모드 버튼**으로 이 기기의 역할을 정합니다. (기기별로 기억됨)
- **✏️ 편집 모드(모바일)**: 일정·투두 추가/수정/삭제 모두 가능
- **📖 읽기 모드(전자책)**: 열람 + 완료 체크만 가능 (추가·수정·삭제 버튼 숨김)

> 전자책(루나2)에서는 앱을 처음 열고 상단 버튼을 눌러 **읽기 모드**로 바꿔두세요.

**수정/삭제 방법(편집 모드)**
- 일간 목록: 각 항목 오른쪽 **🖍️(형광) / ✏️(수정) / 🗑️(삭제)** 아이콘
- 주간 뷰: 칩을 **짧게 탭 = 수정**, **꾹 눌러 = 다른 요일로 이동**
- 수정 팝업 안에서도 **삭제** 버튼 제공

## 기기 간 동기화 (Firebase Firestore)
`firebase-config.js`를 채우면 **모바일과 전자책이 같은 데이터를 공유**합니다.
(비워두면 각 기기 로컬에만 저장 — `⌂ 이 기기에만 저장` 표시)

### 설정 순서
1. [console.firebase.google.com](https://console.firebase.google.com) 에서 **프로젝트 생성**
2. **빌드 → Firestore Database → 데이터베이스 만들기** (위치 선택, 우선 프로덕션 모드로 생성)
3. **빌드 → Authentication → 시작하기 → 로그인 방법 → 익명(Anonymous) 사용 설정**
4. 프로젝트 개요 옆 **⚙️ → 프로젝트 설정 → 내 앱 → 웹(&lt;/&gt;) 앱 추가** → 표시되는 `firebaseConfig` 값 복사
5. 복사한 값을 저장소의 **`firebase-config.js`** 에 붙여넣고 커밋/푸시 (Vercel 자동 재배포)
6. **Firestore → 규칙(Rules)** 탭에 이 저장소의 **`firestore.rules`** 내용을 붙여넣고 **게시**

설정이 끝나면 상단에 `☁︎ 동기화 켜짐` 이 표시되고, 한쪽에서 바꾸면 다른 기기에 곧 반영됩니다.
(전자책은 e-ink라 화면 갱신에 잠깐 지연이 있을 수 있습니다.)

> ⚠️ `⚠︎ 동기화 오류(설정 확인)` 가 뜨면 대개 **익명 로그인 미설정** 또는 **보안 규칙 미게시**가 원인입니다. 위 3·6번을 확인하세요.
> 보안 규칙은 "로그인한 사용자만 접근"까지만 잡아둔 개인용 기준입니다. 더 엄격히 하려면 사용자별 데이터 분리/App Check 추가를 검토하세요.

## 배포 & 전자책(루나2)에 앱으로 설치하기

### 1) Vercel로 배포
빌드 과정이 없는 정적 사이트라 그대로 올리면 됩니다.

**방법 A — GitHub 연동(권장, 이후 자동 배포)**
1. [vercel.com](https://vercel.com) 에 GitHub 계정으로 로그인
2. **Add New → Project** → 이 저장소(`calendar-todo-app`) 선택 → **Import**
3. Framework Preset은 **Other**(빌드 명령 없음), Root/Output 기본값 그대로 → **Deploy**
4. 완료되면 `https://<프로젝트이름>.vercel.app` 주소가 생성됩니다. 이후 이 브랜치에 push하면 자동 재배포됩니다.

**방법 B — CLI**
```bash
npm i -g vercel
vercel        # 최초 배포(질문에 기본값 엔터)
vercel --prod # 운영 배포
```

### 2) 루나2(안드로이드 기반 전자책)에 앱으로 설치
루나2는 안드로이드 기반이라 크롬 등 브라우저에서 PWA를 홈 화면에 설치할 수 있습니다.
1. 단말기를 Wi-Fi에 연결하고 **크롬(또는 내장 브라우저)** 실행
2. 배포된 주소 `https://<프로젝트이름>.vercel.app` 접속
3. 브라우저 **⋮ 메뉴 → "홈 화면에 추가" / "앱 설치"(Install app)** 선택
4. 홈 화면에 생긴 **캘린더 아이콘**을 누르면 주소창 없는 전체 화면 앱으로 실행됩니다.
5. 한 번 열어두면 서비스워커가 캐시하므로 **오프라인에서도** 열람 가능합니다.

> 내장 브라우저에 설치 메뉴가 없으면 Play스토어/APK로 **크롬**을 설치한 뒤 위 과정을 진행하세요.
> e-ink 화면 잔상이 신경 쓰이면 단말기의 새로고침(리프레시) 모드를 함께 사용하세요.

## 현재 구현된 기능
- 일간 / 주간(월~금) / 월간 뷰 전환
- 일정·투두 추가 모달 (날짜·시작시간 기본값 자동, 시간 수정 가능)
- 5단계 중요도 색상 (빨강/주황/노랑/연두/회색)
- 알림 시간 선택 (정시 ~ 1일 전, 네이버 캘린더 방식)
- 미완료 항목 자동 이월 + 지연일수에 따라 첨자 날짜 크기/굵기 증가
- 형광펜 강조 표시
- 주간뷰에서 항목 꾹 눌러 다른 요일로 드래그 이동, 화면 가장자리에서 2초 대기 시 주간 전환

## 다음 단계 (실제 서비스화 시)
1. **Outlook 연동**: Azure App 등록 → Microsoft Graph API `/me/calendarview` (읽기 전용, `Calendars.Read` 스코프)로 `EVENTS` 객체를 채우는 부분만 교체
2. **투두 저장소**: 지금은 메모리(JS 변수)에만 있음 → 실제 서비스에서는 DB 필요
3. **휴대폰 알림**: 일정 시작 전 / 투두 마감 전 알림을 서버에서 계산해 ntfy.sh, Pushover, 또는 자체 푸시(Firebase)로 전송
4. **e-ink 디바이스 연동**: ESP32 + e-paper 패널이 이 화면을 주기적으로 이미지로 렌더링해 가져가는 구조

`script.js` 상단의 `EVENTS`, `TODOS` 객체 형태만 유지하면 위 단계들을 순차적으로 붙일 수 있습니다.
